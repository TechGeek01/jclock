{"google":"","note":"Don't delete this file! It's used internally to help with page regeneration.","body":"# jClock\r\nThis little clock can be used as both a 12- or 24-hour display, as well as a trigger for time-based events on your website.\r\n\r\n## Implementation\r\nTo implement this script, simply stick `<script type=\"text/javascript\" src=\"https://raw.github.com/TechGeek01/jclock/master/jclock.js\"></script>` into the header of your webpage, change your `<body>` tag's  `onload` attribute to `startTime();`, and you're good to go!\r\n\r\n## Usage\r\nTo use jClock, you can interact with the script in one of two ways.\r\n\r\n### Display the clock\r\nSet the `id` attribute of a container to either `12clock` (12-hour clock) or `24clock` (24-hour clock), and jClock does the rest.\r\n\r\n### Trigger timed events\r\nIf your site has JavaScript-handled events, you can trigger them with jClock simply by using\r\n``nesting your action within an if statement, and using one or more of the variables `year`, `month`, `date`, `h24`, `m`, or `s` as the trigger.","name":"jClock","tagline":"A simple JavaScript clock that can be used as a display on websites as well as a trigger for timed events."}